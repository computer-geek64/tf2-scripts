// Scout

// Reset class-level binds
exec reset.cfg

// Drink bonk
alias +bonk "slot3; wait 30; slot2; +attack"
alias -bonk "-attack; say BONK!"
bind mouse3 "+bonk"

// Throw
alias +throw "slot2; +attack"
alias -throw "-attack; lastinv"
bind mouse4 "+throw"
